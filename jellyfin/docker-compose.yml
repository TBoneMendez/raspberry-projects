version: "3.8"
services:
  jellyfin:
    image: jellyfin/jellyfin:latest
    container_name: jellyfin
    #network_mode: host
    ports:
      - "8096:8096"
    volumes:
      - ./config:/config
      - /mnt/c/Users/OPTHODEG/Documents/mediacenter:/media
    restart: unless-stopped
